#a tree with n-nodes will have n-1 edges.
#a graph is also a tree,if and only if vertices are connected.
#edge is the connection link btw nodes.
#parent mode-a node having more than one child.
#degree of a node==total number of children,known as degree
#degree of a tree==it is the degree of that node having maximum number of childs
##internal nodes==node which has atleast one children
##leaf node==node which do not have any child
#levels :- In tree each step from top to bottom is known as levels .
#height of a node :-  longest distance from that node to leaf node (total no of edges in that path)
# Height of a tree :-- from root node to leaf node highest path 
#Depth/level of a tree :- number of edges from node to rood node 
#SubTree:-parts of tree or the  small tree inside the tree .
#Types of a Tree:-
1. Regular Tree
2. Binary Tree :where each node have atmost 2 children . 
3. Binary Search Tree :- the root node  > left node ; root node<right node 
